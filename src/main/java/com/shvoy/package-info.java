/**
 * Application root. Spring Modulith treats each direct sub-package below as an
 * application module with its own verified boundary; code placed directly in
 * this root package (not in a sub-package) is shared across all modules.
 */
package com.shvoy;
